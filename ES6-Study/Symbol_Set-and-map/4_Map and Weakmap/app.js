// new map() 은 key, value 2개를 사용할 수 있다.
// map 에는 set() - key,value 추가, has() - key 확인(true,false 반환), get() - key 값 확인 시 value를 알려줌, size() = length 가 있다
// WeakMap 도 weakSet 이랑 동일하다
// key 값(value)을 덮어 씌울 수 있음

const map = new Map();
